version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.12
    steps:
      - checkout
      - run: GO111MODULE=on go run main.go
  test:
    docker:
      - image: circleci/golang:1.12
    steps:
      - checkout
      - run: echo hello
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test
